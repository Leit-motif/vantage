// A plain net10.0 project, so the SDK's implicit usings already cover System.IO. The test
// framework is here for the assertion failures the fixtures raise on a caller's behalf.
global using Microsoft.VisualStudio.TestTools.UnitTesting;
